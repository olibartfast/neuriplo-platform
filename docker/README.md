# Docker

Docker assets in this repository should support cross-repository integration
workflows, not replace runtime-specific Dockerfiles owned by implementation
repositories.

Candidate assets:

- Compose files for local integration scenarios.
- Test images that pin compatible repository versions.
- Minimal smoke-test environments for CI.
